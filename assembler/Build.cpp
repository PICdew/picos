#include "Build.h"

DString Build::todo = DString("TODO: \n") +
  DString("0. Add history to Parser.")+
DString("1. Fix Parser to accept a blank entry of only a RETURN") +
DString("2. Add log.") +
DString("3. Add parentheses to a+b so that (a+b)*c is used instead of a+b*c");


