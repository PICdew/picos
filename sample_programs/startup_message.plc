/**
 * This is a sample program that will display the os version (1.0.0)
 */
def <main>
{
  clear();
  sprint("PicOS vs");
  putd(kversion(0));
  putch('.');
  putd(kversion(1));
  putch('.');
  putd(kversion(2));
}
