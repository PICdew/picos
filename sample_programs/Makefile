

all:
	pasm -o fs/time time.plc
	pasm -o fs/start startup_message.plc
	pasm -o fs/setdate setdate.plc
	pasm -o fs/settime settime.plc
	pasm -o fs/test test.plc	
	pasm -o fs/morse morse.plc
	pasm -o fs/ed ed.plc
	pasm -o fs/timestamp timestamp.plc
	pasm -o fs/calc calc.plc