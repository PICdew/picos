/**
 * Reads text input and produces Morse Code.
 */
def <main>
{
  sprint("Morse: ");
  m = getch();
  while(m != 0x4)
    {
      putch(m);
      morse(m);
      m = getch();
    }
  clear();
  sprint("Done.");
}
