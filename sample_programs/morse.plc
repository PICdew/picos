/**
 * For a character supplied to the command line, the Morse code is generated
 */
def <main>
{
  sprint("Morse: ");
  m = getch();
  while(m != 0x4)
    {
      putch(m);
      morse(m);
      m = getch();
    }
  clear();
  sprint("Done.");
}
