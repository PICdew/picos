/**
 * This program reads input and stores it in the mounted filesystem's raw file.
 */
def <main>
{
  a = getch();
  fclear();
  while(a != 4)
    {
      fputch(a);
      putch(a);
      a = getch();
    }
  clear();
  fflush();
  sprint("Done");
}
